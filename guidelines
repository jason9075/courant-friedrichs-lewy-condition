courant friedrichs lewy condition
---

## 1. 核心視覺：動態波動圖（The Live Wave）

這是網頁的主角。不要只給靜態的圖，要用 Canvas 或 Chart.js 畫出一個動態前進的波。

* **雙圖對比（推薦）**：
* **圖 A（解析解/真實物理）**：一個完美的方形波（或高斯脈衝）永遠不變形地向右移動。
* **圖 B（數值解/模擬結果）**：根據使用者設定的參數，用有限差分法（也就是你上面那段 Python 轉成 JavaScript）即時計算並畫出的波。


* **視覺特效**：當 $\sigma \le 1$ 時，數值解的波會平滑移動（伴隨一點點數值耗散的圓角）；當使用者把參數調到 $\sigma > 1$ 的瞬間，圖 B 的波形要開始劇烈上下震盪，最後**線條直接飛出畫布邊界（代表發散）**，並畫面上彈出紅色警告：「⚠️ 系統崩潰！數值已發散至無限大！」

---

## 2. 核心控制項：Courant 數儀表板

提供三個拉桿（Slider）讓使用者任意調整，並在拉桿下方**即時計算並顯示 $\sigma$ 值**。

* **拉桿 1**：波速 $c$（控制波走多快）
* **拉桿 2**：時間步長 $\Delta t$（控制每次模擬跨越的時間）
* **拉桿 3**：網格點數 $N$（間接控制 $\Delta x = L/(N-1)$）

### 💡 決定成敗的視覺巧思：CFL 交通號誌

在拉桿旁邊放一個醒目的數值顯示：

$$\sigma = \frac{c \Delta t}{\Delta x} = \text{目前數值}$$

* 當 $\sigma < 1$：背景顯示**綠色**，標註「穩定（Stable）」。
* 當 $\sigma = 1$：背景顯示**黃色**，標註「臨界點（Critical）」。
* 當 $\sigma > 1$：背景顯示**紅色閃爍**，標註「不穩定（Unstable）」。
* *進階玩法*：提供一個「Auto-fix」勾選框，勾選後，當使用者調大 $c$ 或縮小 $\Delta x$ 時，網格會**自動縮小 $\Delta t$** 來維持綠燈，讓使用者體驗「動態時間步長（Adaptive Time-stepping）」的威力。

---

## 3. 微觀視角：資訊傳播的物理動畫（The Micro-View）

光看大圖，使用者可能還是不懂「為什麼會炸開？」。你需要一個網格微觀動畫來解釋**物理因果連續區（Domain of Dependence）**。

* **網格示意圖**：在畫布下方畫一排離散的「點」（代表網格 $x_{i-1}, x_i, x_{i+1}$）。
* **物理資訊箭頭**：用一個**藍色陰影扇形**從 $x_i$ 往下一時間步延伸，代表「真實物理世界中，資訊在 $\Delta t$ 時間內最遠能走到的距離（$c \Delta t$）」。
* **數值依賴網格**：用**紅色點和線**連接 $x_{i-1}$ 到 $x_i$，代表「演算法（數值網格）能抓到的資訊範圍（$\Delta x$）」。
* **動態展示**：
* 當使用者調大 $\Delta t$ 導致藍色陰影（物理）超出了紅色網格（數值）的範圍時，網頁顯示文字解釋：*「看！物理訊號已經跑到下一個網格去了，但你的程式還在用舊的鄰居資料計算，程式抓不到未來的資訊，所以它瞎了，這就是崩潰的原因！」*



---

## 4. 關卡式教學引導（Scenarios / Presets）

不要一開始就丟給使用者一堆拉桿，他們會迷失。設計幾個「一鍵套用」的導覽情境：

1. **情境一：完美的平衡 ($\sigma = 1$)**
* 點擊後，設定成最完美的狀態，波形完美移動。


2. **情境二：數值耗散的代價 ($\sigma = 0.1$)**
* 點擊後，把 $\Delta t$ 調極小。使用者會發現波雖然很穩定，但方形波前進幾步後變成了「小山丘」（數值擴散）。藉此解釋：$\Delta t$ 不是越小越好，太小會算太慢且會失真。


3. **情境三：蝴蝶效應的崩潰 ($\sigma = 1.05$)**
* 點擊後，只比穩定條件多一點點。使用者會看到波形一開始很正常，但過了幾秒後，鋸齒狀震盪突然從邊界長出來，然後瞬間爆炸。這能完美展示顯式格式的「不穩定性」是如何累積的。



